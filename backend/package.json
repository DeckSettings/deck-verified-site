{
  "name": "deck-verified-api",
  "version": "1.0.0",
  "author": "Josh.5",
  "license": "MIT",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "dev-local": "env $(grep -v '^#' ../.env | xargs) nodemon --watch './**/*.ts' --exec 'tsx' index.ts",
    "serve-local": "env $(grep -v '^#' ../.env | xargs) pm2-runtime --interpreter tsx index.ts",
    "build": "tsc",
    "serve": "node dist/index.js",
    "serve-no-tasks": "pm2-runtime -i 2 dist/index.js --no-scheduled-tasks",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "logfmt": "^1.4.0",
    "pm2": "^5.4.3",
    "redis": "^4.7.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
